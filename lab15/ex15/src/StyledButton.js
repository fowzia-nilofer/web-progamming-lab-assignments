import React from "react";

function StyledButton(){
    const styleButton = {
        backgroundColor: "red",
        padding: 7,
        fontSize: "20px"
    };
}

export default StyledButton;